export type FilterOptions = {
    category: string[];
    minPrice: number;
    maxPrice: number;
    inStock: boolean;
};

export const categories = ['Tất cả', 'Nước mắm cốt', 'Nước tương', 'Tương ớt', 'Tương cà', 'Nước chấm'];
